# Feature Flags
# Set to "true" to enable the unified prioritization (hybrid loop) engine (default rollout state).
# Set to "false" to force the legacy prioritization path during the gradual rollout or for rollback.
NEXT_PUBLIC_USE_UNIFIED_PRIORITIZATION=true
# Optional server-side alias for the same flag (kept for backward compatibility)
USE_UNIFIED_PRIORITIZATION=true
